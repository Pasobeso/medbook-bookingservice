name: 01-docker-publish

on:
  push:
    branches: [main]
    tags: ["v*.*.*"]
  pull_request:
    branches: [main]

# set github permissions
permissions:
  contents: read
  packages: write

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository_owner }}/medbook-bookingservice

jobs:
  build-and-push:
    runs-on: ubuntu-lastest
    concurrency:
      group: docker-{{ github.ref }}
      cancel-in-progress: true
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Show Dockerfile # Fail fast
        run: ls -la && test -f Dockerfile

      - name: Set up Docker Buildx
        uses: dokcer/setup-buildx-action@v3

      - name: Log in to ghcr
        uses: docker/login-action/v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Docker metadata (tags & labels)
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=ref,event=branch
            type=ref,event=tag
            type=sha
            type=raw,value=latest,enable=${{ github.ref == 'refs/heads/main' }}

      - name: Build & (conditionally) push amd64 only
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          platforms: linux/amd64
          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ steps.meta.output.tags }}
          labels: ${{ steps.meta.output.labels}}
          cache-from: type=gha
          cache-to: type=gha,mode=max
